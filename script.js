// 'use strict';

// // const endDate = Date.parse("25 Aug 2023 05:12:00");
// const endDate = (Date.now() + 1209600000);
// console.log(endDate);

// let currentTime = Date.now(); // it returns milliseconds
//   console.log(currentTime);

// let difference = endDate - currentTime;
// console.log(difference);
// let days = 0;
// let hours = 0;
// let minutes = 0;
// let seconds = 0;
// // const currentTime = Date.now(); // it returns milliseconds
// // console.log(currentTime);

// // let difference = endDate - currentTime;
// // console.log(difference);
// // let days = 0;
// // let hours = 0;
// // let minutes = 0;
// // let seconds = 0;
// // days = (difference / (24 * 60 * 60 * 1000));
// // difference = (difference % (24 * 60 * 60 * 1000));
// // console.log(Math.floor(days));
// // hours = (difference / (60 * 60 * 1000));
// // difference = (difference % (60 * 60 * 1000));
// // minutes = (difference / (60 * 1000));
// // difference = (difference % (60 * 1000));
// // seconds = (difference / (1000));
// // difference = (difference % (1000));

// // console.log(`no of days is ${days} and hrs : ${hours} min ${minutes} sec ${seconds}`)
// setInterval(()=> {
// days = (difference / (24 * 60 * 60 * 1000));
// difference = (difference % (24 * 60 * 60 * 1000));
// console.log(Math.floor(days));
// hours = (difference / (60 * 60 * 1000));
// difference = (difference % (60 * 60 * 1000));
// minutes = (difference / (60 * 1000));
// difference = (difference % (60 * 1000));
// seconds = (difference / (1000));
// difference = (difference % (1000));

//   console.log(`no of days is ${Math.floor(days)} and hrs : ${Math.floor(hours)} min ${Math.floor(minutes)} sec ${Math.floor(seconds)}`)
// },1000)